apiVersion: v1
kind: ConfigMap
metadata:
  name: pesa-config
  namespace: pesa-analyser
data:
  APP_NAME: "Pesa Analyser"
  APP_VERSION: "1.0.0"
  ENV: "production"
  MPESA_ENVIRONMENT: "production"
  REDIS_HOST: "redis-service"
  DATABASE_URL: "postgresql://postgres:postgres@postgres-service:5432/pesa"
